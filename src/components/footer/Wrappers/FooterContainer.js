const FooterContainer = ( { children } ) => (
    <div className="flex items-center justify-between bg-transparent p-6 bottom-0 left-0 right-0 max-w-[1920px] mx-auto absolute">
        {children}
    </div>
)

export default FooterContainer;